{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaireservice</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaireService.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('services_commentaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('services_commentaire_edit', { 'id': commentaireService.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
