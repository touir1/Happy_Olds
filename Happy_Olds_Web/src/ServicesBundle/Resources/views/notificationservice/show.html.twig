{% extends 'base.html.twig' %}

{% block body %}
    <h1>Notificationservice</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notificationService.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('services_notification_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('services_notification_edit', { 'id': notificationService.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
