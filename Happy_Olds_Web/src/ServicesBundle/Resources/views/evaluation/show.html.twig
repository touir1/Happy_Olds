{% extends 'base.html.twig' %}

{% block body %}
    <h1>Evaluation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ evaluation.score }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('services_evaluation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('services_evaluation_edit', { 'id': evaluation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
